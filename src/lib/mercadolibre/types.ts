export interface MercadoLibreOrder {
  order_id: string;
  client_id: string;
  publicacion_id: string;
  fecha: string;
  estado: string;
  unidades: number;
  // ... (resto de las propiedades) ...
}
